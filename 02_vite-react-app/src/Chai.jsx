function Chai() {
  return <h2> chai aur react</h2>;
}

export default Chai;

/** 
 * Vite will take a strict check on the conventions like name of a component should be save with .jsx extension while reactjs will give flexibility in this
 
 * But, the name of function inside the component should be the start with the uppercase letter.
 */
